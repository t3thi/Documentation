---
id: K-000029
kind: recommendation
state: active
maturity: supported
title: Synthetic Language-All sentinels should be removed from backend selection
topic: language-identity
responsibilities:
  - language-identity
  - synchronization-intent
  - editorial-workflow
  - compatibility-migration
summary: >
  The initiative's current direction is to represent backend display and
  filter selections through explicit concrete language choices instead of a
  synthetic -1 sentinel, while retaining -1 during migration only when an
  editor deliberately stores Language All on a record as synchronization
  intent; each Core call path must be classified and verified before removal.
created: 2026-09-04
updated: 2026-09-25
evidence:
  - source: transcript:2026-09-04
    location: "14:35-28:26, semantic distinction and Site call-path investigation"
    relation: establishes
    summarized_in: minutes:2026-09-04#topic-2-semantic-scope-of-the-all-languages-marker
  - source: transcript:2026-09-04
    location: "41:38-49:57, Recycler selector comparison and explicit language selection"
    relation: supports
    summarized_in: minutes:2026-09-04#topic-4-recycler-language-selector-consistency
  - source: core:language-all-call-paths-2026-09-04
    location: >
      Site::getAvailableLanguages, PageLayoutContext,
      SiteLanguageAccessorTrait, Workspaces ReviewController, Recycler and
      LinkAnalyzer
    relation: supports
    note: >
      Current Core still has active synthetic and record-level -1 call paths;
      the inspection confirms the distinction but not that every synthetic
      occurrence can already be removed safely.
  - source: transcript:2026-09-18
    location: "16:38-23:29, LinkAnalyzer record language and fallback review"
    relation: refines
    summarized_in: minutes:2026-09-18#topic-4-linkanalyzer-language-semantics
  - source: transcript:2026-09-18
    location: "23:36-53:53, ContentObjectRenderer review"
    relation: supports
    summarized_in: minutes:2026-09-18#topic-5-frontend-language-restrictions-in-contentobjectrenderer
  - source: transcript:2026-09-18
    location: "53:54-58:47, CategoryCollection review"
    relation: supports
    summarized_in: minutes:2026-09-18#topic-6-category-and-extbase-persistence-findings
  - source: transcript:2026-09-18
    location: "58:47-1:04:52, Extbase Typo3DbQueryParser review"
    relation: supports
    summarized_in: minutes:2026-09-18#topic-6-category-and-extbase-persistence-findings
  - source: core:language-all-call-paths-2026-09-18
    location: >
      LinkAnalyzer, ContentObjectRenderer::getLanguageRestriction,
      CategoryCollection::getCollectedRecords and
      Typo3DbQueryParser::getLanguageStatement
    relation: confirms
    note: >
      Current Core confirms three persisted-record lookup paths and one
      LinkAnalyzer path that combines the record language with a fallback
      carrying a separate meaning.
  - source: transcript:2026-09-25
    location: "17:02-29:28, interim marker scope across persisted and synthetic contexts"
    relation: refines
    summarized_in: minutes:2026-09-25#topic-2-scope-and-semantics-of-the-language-all-marker
  - source: transcript:2026-09-25
    location: "36:32-49:53, plausibility review of the patch occurrences"
    relation: supports
    summarized_in: minutes:2026-09-25#topic-3-patch-coverage-and-human-review-method
relations:
  - type: depends_on
    target: K-000006
  - type: supports
    target: K-000007
  - type: related_to
    target: K-000017
  - type: related_to
    target: K-000028
  - type: related_to
    target: D-000002
external_artifacts:
  - id: gerrit:95619
    relation: related_to
    state: open-wip
    verified_at: 2026-09-25
  - id: gerrit:92267
    relation: related_to
    state: open-wip
    verified_at: 2026-09-04
review:
  status: review-required
---

# Synthetic Language-All sentinels should be removed from backend selection

## Statement

The initiative's current direction is to eliminate synthetic `-1` values from
backend display, comparison and filter selection wherever the intended state
can be expressed as an explicit set of concrete languages. TYPO3 v15 already
offers a multi-select for comparison-language overlays, so selecting every
available overlay does not inherently require a fictitious additional
language. During migration, `-1` should remain associated only with an editor
deliberately choosing Language All in a record's language field. That stored
value represents record-wide synchronization intent rather than a human
language identity.

## Rationale

The same integer currently covers two different contracts: a value persisted
on a record and a synthetic backend option. A shared marker can make the code
more readable and searchable without resolving that semantic collision. For
the bounded marker patch, using one constant wherever `-1` means All Languages
is compatible with keeping the contracts conceptually distinct. Expressing UI
and filter selection as concrete IDs remains the longer-term direction where
the selected scope permits it, while reserving the persisted value for the
record-level intent preserves the meaning that must later be migrated to an
explicit synchronization model.

## Consequences

No occurrence may be removed solely because it contains `-1`. Each call path
must first be classified as a synthetic selection, an actual lookup of
Language-All records, or a fallback with a third meaning. Current Core still
uses a synthetic `SiteLanguage(-1)` in Layout, translates `-1` to the
Workspaces filter value `all`, and includes persisted Language-All records in
routing and sitemap queries. The review on 2026-09-18 additionally confirmed
that `ContentObjectRenderer`, the frontend `CategoryCollection` and Extbase's
`Typo3DbQueryParser` deliberately select records stored with Language All.
Those are valid record-level marker uses. `LinkAnalyzer`, by contrast, uses
the record's actual language where available and also falls back to `-1` when
no usable record language exists. That fallback needs a distinct explicit
contract or representation before it can be changed. The implementation
sequence therefore requires characterization tests and explicit replacement
states before synthetic handling can be retired. This recommendation does not
yet change Core behavior, deprecate `sys_language_uid = -1`, or select the
final persisted synchronization contract.

For Gerrit 95619 specifically, the initiative agreed on 2026-09-25 not to
encode the persisted, synthetic and fallback meanings in separate constants
yet. Every occurrence still requires a plausibility check that `-1` actually
means All Languages, and the marker description must not imply that the
constant is limited to database language fields. This interim naming decision
does not make the underlying contracts equivalent, prevent a later split or
weaken the long-term replacement direction above.
